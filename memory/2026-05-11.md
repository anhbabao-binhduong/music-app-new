# Memory log - 2026-05-11

- RSS news fetching for web was switched from CORS proxy to direct HTTP in `lib/data/repositories/news_repository_impl.dart`.
- Updated Zing News RSS source from `https://zingnews.vn/rss/giai-tri.rss` to `https://znews.vn/rss/giai-tri.rss` (301 redirect destination confirmed by terminal test).
- Added web launcher scripts:
  - `run_web.bat`
  - `run_web.sh`
- README updated with a dedicated **Chạy app** section instructing:
  - Windows: run `run_web.bat`
  - Mac/Linux: run `run_web.sh`
- Fixed RSS text decoding:
  - `lib/data/repositories/news_repository_impl.dart` now decodes feed bytes with `utf8.decode(response.bodyBytes)` to avoid mojibake from Zing News.
  - `lib/data/models/news_article_model.dart` now decodes common HTML entities for `title` and `description`, fixing Thanh Niên text like `&#039;`, `&ugrave;`, `&atilde;`, `&ograve;`.
- Verified with `dart run test/debug_rss_encoding.dart`:
  - Zing News title/description render correctly in Vietnamese.
  - Thanh Niên title/description render correctly without leftover HTML entities.