# 2026-05-21

- Added in-app news article reading flow for `lib/pages/news/news_page.dart`.
- Reused existing `lib/pages/news/news_webview_page.dart` instead of opening external browser.
- Updated news item tap handler to push `NewsWebViewPage` with article `url` and `title`.
- Verified with `flutter analyze`; no new analyzer errors introduced by this change.